public class UserInputs extends SuperDetails{
    public UserInputs(String name, int meter,int amp, int prev, int curr, int year, int month, int date){
        setName(name);
        setMeter(meter);
        setAmps(amp);
        setPrev_unit_num(prev);
        setCurr_unit_num(curr);
        setYear(year);
        setMonth(month);
        setDate(date);
    }

}
